- [API codegen + vite](api-codegen-vite-transient-errors.md) — running api-spec codegen briefly deletes generated files; transient vite "Failed to load url …/api.ts" errors are harmless, restart the web workflow if they persist.
- [Analysis response contract](analysis-response-contract.md) — analyze/news/review responses returned raw & consumed as `any`; keep prompt JSON keys, UI fields, and OpenAPI schemas in lockstep by hand.
- [Stripe entitlement from metadata](stripe-entitlement-from-metadata.md) — resolveAccess reads plan/credits from live Stripe product metadata; mutating/reusing a product re-entitles current subs. Keep subscription tiers clamped to the OpenAPI plan enum.
- [Post-merge DB push & drizzle rename prompt](post-merge-db-push.md) — merges that add tables may need `db push`; an orphan table not in schema makes push hang on a no-TTY rename prompt (even `--force`); drop the empty orphan to unblock.
- [Chart bias vs qualification](chart-bias-vs-qualification.md) — analyze/review prompts must set bias from price direction, kept separate from whether the setup qualifies; read drawn annotations, don't infer direction from candle color.
- [AI JSON truncation & screenshot intake](ai-json-response-truncation.md) — "charts won't read" was JSON overflowing max_tokens, not image reading; budget tokens, tolerant-parse, downscale client-side.
- [Mutation side effects on unmount](mutation-side-effects-on-unmount.md) — tabs unmount on switch; persistence/journaling for an in-flight request must use hook-level useMutation callbacks (built from data+variables), not mutate-scoped ones.
- [Stripe wiring gotchas](stripe-replit-sync-gotchas.md) — Replit Stripe connection uses `settings.secret` (not `secret_key`); `stripe-replit-sync` must be esbuild-external or runMigrations silently creates 0 tables.
- [Task-completion validation misattribution](task-completion-validation-misattribution.md) — completion code review can judge the wrong task over an over-broad diff; verify `git show --stat HEAD` scope, then skip with evidence.
